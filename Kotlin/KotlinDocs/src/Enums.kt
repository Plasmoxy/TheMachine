
private enum class Semafor {
	GO, STOP, PREPARE
}

private enum class Color(val rgb: Int) {
	RED(0xFF0000),
	GREEN(0x00FF00),
	BLUE(0x0000FF)
}

private open class Asdasd

fun main(args: Array<String>) {
	val color = Color.RED.rgb
}